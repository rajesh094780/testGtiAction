name: add-mask-test
on: 
  workflow_dispatch:
    inputs:
      secret:
        description: 'secret value'
        required: true
env:
  TEST_RESULT: 'HELLO'
jobs:
  my-job:
    runs-on: ubuntu-latest
    outputs:
      ssssss: ${{ steps.set-variable.outputs.a }}
    steps:
      - name: add-mask test
        id: set-variable
        run: |
         
          echo "${{secrets."${{env.TEST_RESULT}}"}}"
  my-job1:
    runs-on: ubuntu-latest
    needs: [my-job]
    steps:
      - name: add-mask testsdsd
        run: |
          
          echo "${{needs.my-job.outputs.ssssss}}"
