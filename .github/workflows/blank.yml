name: SQl DB REFRESH

on:
  workflow_dispatch:
    inputs:      
      sourceServer:
        description: 'Provide Source Server'
        required: true
      targetServer:
        description: 'Provide Target Server'
        required: true
env:
  CLUSTER_NAME: ${{ github.event.inputs.sourceServer }}
  
  
jobs:
  publish:
    runs-on: ubuntu-latest
   
    steps:
      - name: Get Azure credential
        id: get-cred
        run: |
          env_name=${{ github.event.inputs.sourceServer }}
          if [[ $env_name == abc ]]; then
            echo "::set-output name=login_cred::${{ secrets.MULE_AES_SECRET_KEY_PASSWORD }}"
          fi
        if: env.CLUSTER_NAME == 'PSR'
          
 
